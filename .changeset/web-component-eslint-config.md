---
"@xsynaptic/eslint-config": minor
---

Add an opt-in `getWebComponentConfig(files)` export with best-practice rules for authoring native web components (custom elements). `eslint-plugin-wc` is now bundled as a dependency.
