# satteri-auto-import

`<Quotation>` is used below with no import statement. The plugin injects the import at build time.

<Quotation cite="Philip K. Dick">
	Reality is that which, when you stop believing in it, doesn't go away.
</Quotation>
